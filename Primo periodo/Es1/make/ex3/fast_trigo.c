#include "fast_trigo.h"
#include <math.h>

float fast_sin(float x) {
    return sinf(x);  // Utilizza la funzione seno dalla libreria math
}

float fast_cos(float x) {
    return cosf(x);  // Utilizza la funzione coseno dalla libreria math
}